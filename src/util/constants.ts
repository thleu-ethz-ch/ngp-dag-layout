const CONNECTOR_SIZE = 10;
const CONNECTOR_SPACING = 10;
const EPSILON = 1e-10;
const DEBUG = false;
const ROOT_DIR = "ngp-dag-layout";

export {
    CONNECTOR_SIZE,
    CONNECTOR_SPACING,
    EPSILON,
    DEBUG,
    ROOT_DIR,
};
